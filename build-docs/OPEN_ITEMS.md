# Open Items

| Item | Status | Notes |
|---|---|---|
| Keep the memory layer current during future work | open | Add dated log entries, record locked decisions, and update blockers as they appear. |
| Version 1.5 catalog UI and local adapter | ready for review | Branch `feature/v15-catalog-ui`: import report, search, manual items, recent selections, rollback, and existing item-form population are implemented and locally verified. |
| Approve a sanitized production catalog CSV | owner input needed | Confirm real headers, typical row/file size, encoding, required fields, UOM values, and whether costs/prices are per listed UOM. |
| Confirm duplicate SKU policy | provisional | Current foundation rejects later duplicate normalized SKUs and reports each rejected row. |
| Validate catalog storage volume | pending sample | Measure the real CSV before publishing a supported local row/file-size limit. |
