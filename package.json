{
  "name": "gtm-calc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "node --check js/main.js && node --check js/domain/calculations.js && node --check js/domain/formatters.js && node --check vite.config.js"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "devDependencies": {
    "vite": "8.1.4",
    "vitest": "4.1.10"
  }
}
